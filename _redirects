# These tell Cloudflare Pages to pass these URLs to functions/[[path]].js
# instead of trying to serve them as static files (which would 404).
# Status 200 = transparent rewrite (URL stays the same in the browser).

/scoper.lua            /index.html    200
/cracked.lua           /index.html    200
/commandlibrary.lua    /index.html    200
/animlist.lua          /index.html    200
/nopride.lua           /index.html    200
/nopride2.lua          /index.html    200
/adonisbypass.lua      /index.html    200
/scripts/list.json     /index.html    200
